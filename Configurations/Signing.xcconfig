// Keep the public project independent of any Apple Developer account while
// retaining the demo's established bundle identifiers for simulator builds.
SWIFT_CHESS_DEMO_BUNDLE_ID_PREFIX = trickfest

// Device builders may copy Signing.local.xcconfig.example to
// Signing.local.xcconfig and set account-specific values there.
#include? "Signing.local.xcconfig"
